SUBJECT: the math on missed calls at {{ company }}
Hi {{ first_name }},

Just the numbers this time.

Most {{ trade }} companies miss about {{ missed_calls_week }} calls a week. At an average job of ${{ job_value }}, that is ${{ weekly_loss }} a week walking to competitors, over ${{ yearly_loss }} a year.

{{ product_pitch[0] | upper }}{{ product_pitch[1:] }} costs a small fraction of one recovered job and it works nights, weekends, and holidays.

60 second demo: {{ demo_url }}

Worth 15 minutes to keep that revenue? My calendar: {{ calendly_url }}

{{ signature }}
